# properties file with build information. Placeholders are written by maven
project.version=${project.version}
project.groupId=${project.groupId}
project.artefactId=${project.artifactId}
project.name=${project.name}